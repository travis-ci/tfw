language: python
dist: xenial
group: edge
python:
- '3.5'
- '3.6.5'
services:
- docker
cache:
  directories:
  - "${HOME}/bin"
install:
- make deps
script:
- make lint
- touch bin/tfw
- make USAGE.md
- git diff --exit-code
- git diff --cached --exit-code
- make test
- make systest
